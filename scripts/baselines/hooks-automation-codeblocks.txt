```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier --write $CLAUDE_FILE_PATH 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
```
```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier --write $CLAUDE_FILE_PATH 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
```
```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$(date '+%Y-%m-%d %H:%M:%S'): Bash command executed\" >> ~/.claude/audit.log"
          }
        ]
      }
    ]
  }
}
```
```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cat /dev/stdin | grep -qE '(rm -rf|drop table|DROP TABLE|push --force)' && echo 'BLOCKED: 위험한 명령 감지됨' && exit 1 || exit 0"
          }
        ]
      }
    ]
  }
}
```
```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier --write $CLAUDE_FILE_PATH 2>/dev/null || true"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npx eslint --fix $CLAUDE_FILE_PATH 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
```
```bash
# 단일 파일 리뷰
claude -p "src/auth/login.ts를 보안 관점에서 리뷰해줘"

# JSON 출력으로 파싱 가능
claude -p "변경된 파일 목록을 JSON으로 요약해줘" --output-format json
```
```yaml
name: Claude Code Review
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Claude Review
        run: |
          npx @anthropic-ai/claude-code -p \
            "git diff origin/main...HEAD를 분석하고
             보안 취약점, 버그 가능성, 성능 이슈를 한국어로 리뷰해줘.
             각 항목을 심각도(HIGH/MEDIUM/LOW)와 함께 표시해줘." \
            --output-format json > review.json
          cat review.json
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Post Review Comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const review = JSON.parse(fs.readFileSync('review.json', 'utf8'));
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: typeof review === 'string' ? review : JSON.stringify(review, null, 2)
            });
```